src_dir: $master_dir/oasis
cf_name_dir: $src_dir


config_files: 
        namcouple:
        cf_name_table.txt: $cf_name_dir


header:

transformations:
        distwgt: 
                bins1: 15
                bins2: 6
                appendtext: |
#
LOCTRANS SCRIPR
DISTWGT U SCALAR LATITUDE $bins1 $bins2
#
                modify_files:
                        namcouple: '<<--append-- ${appendtext}' 
                                

        bicubic: 
                bins: 15
                appendtext: |
#
LOCTRANS SCRIPR
BICUBIC D SCALAR LATITUDE $bins
#
                modify_files:
                        namcouple: '<<--append-- ${appendtext}' 




methods:
        '<--append--':
                implementations:
                        shell: |
cat >> ${lhs} << EOF
${rhs}
EOF'
                        python: 
open ${lhs}, a
for line in rhs:
        print ${}
close ${lhs}



coupled_vars: 
                config_txt: |
$snd $rcv $id $seq $lag $restart_file
$from_grid $to_grid $from_grid.nx $from_grid.ny $to_grid.nx $to_grid.ny
$transformation
                

